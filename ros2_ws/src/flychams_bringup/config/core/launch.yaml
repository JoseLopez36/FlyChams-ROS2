# Node launch configuration
# Set to [enable, log_level]
# Enable: true launches the node, false does not
# Log levels: debug, info, warn, error, fatal

# ------------------- GLOBAL SETUP NODES -------------------\
# Registrator node
registrator: [true, 'info']
# AirSim node
airsim: [true, 'error']

# ------------------- AGENT SETUP NODES -------------------
# MavROS node
mavros: [true, 'info']

# ------------------- GLOBAL RUN NODES -------------------
# Perception nodes
target_clustering: [true, 'error']
cluster_analysis: [true, 'error']
# Coordination nodes
agent_assignment: [true, 'info']
agent_analysis: [true, 'error']
# Simulation nodes
gui_manager: [true, 'error']
metrics_factory: [true, 'error']
marker_factory: [true, 'error']
target_state: [true, 'error']
target_control: [true, 'error']
# Recording nodes
airsim_record_camera: [false, 'error']

# ------------------- AGENT RUN NODES -------------------
# Control nodes
drone_frames: [true, 'error']
drone_state: [true, 'error']
drone_control: [true, 'error']
camera_frames: [true, 'error']
camera_control: [true, 'error']
# Coordination nodes
agent_positioning: [true, 'error']
agent_tracking: [true, 'error']