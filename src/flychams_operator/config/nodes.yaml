/**/metrics_creator_node:
  ros__parameters:
    # Update rate (Hz)
    update_rate: 10.0

/**/markers_generator_node:
  ros__parameters:
    # Update rate (Hz)
    update_rate: 10.0

/**/annotations_generator_node:
  ros__parameters:
    # Annotation publish rate (Hz)
    annotation_rate: 10.0
    # View dimensions (must match agent stream settings)
    central_view:
      width: 854
      height: 480
    tracking_view:
      width: 427
      height: 240