Use this command to re-stream the UE5 stream:

gst-launch-1.0 -v udpsrc port=5000 caps="application/x-rtp, media=(string)video, payload=(int)96, encoding-name=(string)H265" \
! rtph265depay \
! h265parse \
! queue \
! mpegtsmux alignment=7 \
! udpsink host=127.0.0.1 port=5010 sync=false;