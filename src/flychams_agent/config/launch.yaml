# Node launch configuration:
# - node_allowlist allows only the listed nodes to be launched
# - node_log_levels overrides per-node log level; default is 'error'

# Node allowlist: Only launch these nodes
node_allowlist:
  - agent_frames
  - agent_state
  - agent_control
  - agent_analysis
  - agent_positioning
  - agent_tracking
  # - agent_stream

# Per-node log level overrides
node_log_levels:
  agent_frames: "info"
  agent_state: "info"
  agent_control: "info"
  agent_analysis: "error"
  agent_positioning: "error"
  agent_tracking: "error"
  agent_stream: "error"